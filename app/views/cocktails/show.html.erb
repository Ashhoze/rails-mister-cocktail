<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://blog.ubaldi.com/wp-content/uploads/2019/05/cocktails-blender-1240x503.jpg);">
  <div class="container ">
    <h1>Votre choix: <%= @cocktail.name %></h1>
    <p>*L'abus d'alcool nuit gravement a la santé</p>
  </div>
</div>
<p><% @cocktail.doses.each do |dose| %> </p>
<li><%= "#{dose.description} de " "#{dose.ingredient.name}" %> </li>
<p><%= link_to "Delete ",
            dose_path(dose),
            method: :delete,
            data: { confirm: "Are you sure?" } %></p>
<%end %>


<%= link_to "Ajoutez votre recette" , new_cocktail_dose_path(@cocktail) %>

<%= link_to "Liste", root_path %>
